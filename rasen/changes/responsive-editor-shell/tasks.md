## 1. Implement the pure responsive profile boundary

- [x] 1.1 Add one deep internal `packages/excalidraw/components/responsiveEditorShell/` module containing the exact `PhysicalSafeAreaInsets`, `ResponsiveEditorShellInput`, and `ResponsiveEditorShellProfile` types plus `resolveResponsiveEditorShell(input)`. Keep the closed schema from `design.md`, readonly nested values, fixed field order, schema version 1, and no common/SDK public export, generic rule compiler, registry, or external store.
- [x] 1.2 Implement deterministic input normalization: non-finite/negative dimensions and physical insets become zero, negative zero is removed, safe-area/signature precision is documented and consistent, logical block edges map top/bottom, and only RTL swaps the logical projections of unchanged physical left/right.
- [x] 1.3 Resolve tier from the already host-resolved form factor; map phone to the phone adapter and tablet/desktop to the desktop adapter; resolve strict orientation, the existing 520px short-block boundary, touch density from phone/touch latch/coarse pointer, and mobile/compact/full presentation from tier and desktop preference. Include every semantic output in the stable signature.
- [x] 1.4 Add pure resolver tests for the 1440×900, 1180×700, 1024×768, 768×1024, 812×375, 640×480, and 375×812 matrix plus immediate neighbors of every phone-width, mobile-landscape width/height, tablet min/max, 520px short-block, sidebar-fit, and width-equals-height boundary.
- [x] 1.5 Extend the resolver suite for host-resolved tier/sidebar inputs, NaN/infinity/negative/negative-zero values, precision-equivalent inputs, asymmetric LTR/RTL physical and logical safe areas, deterministic signature field coverage, immutability, and semantic equality.

## 2. Deepen the existing App/context lifecycle

- [x] 2.1 Type the existing App/context value as a private intersection of the unchanged common `EditorInterface` with `responsive`, initialize it, preserve and synchronize every existing compatibility field, and expose a deep internal `useResponsiveEditorShell()` projection through the existing provider. Keep the common type/barrel and public `getEditorInterface` return contract unchanged; add no second provider, public package export, `LayerUIProps` field, or memo-comparator clause.
- [x] 2.2 Refactor `refreshEditorInterface()` to accept/read one container rect, host form factor, dock breakpoint, that App instance's requested-language direction, touch latch, `(pointer: coarse)` match, and canonical safe areas; call the pure resolver and reuse the previous profile object whenever its signature is unchanged. Project/read direction on the editor root and remove `document.documentElement.dir` as responsive-policy authority without redesigning the existing global translation catalog.
- [x] 2.3 Keep the existing App container `ResizeObserver` as the only container-size observer and prove an embedded container resize refreshes profile/adapter without a window resize. Retain the last valid positive size-derived profile through detach or transient zero bounds, use the deterministic initial desktop profile before the first valid measurement, and resume on reattachment.
- [x] 2.4 Resolve and record instance direction, update the editor root, and refresh the interface after `setLanguage()`; also refresh on owner-window/available `visualViewport.resize`, so fixed-size direction and safe-area changes reach context consumers. Register all listeners per App instance through the existing cleanup mechanism; do not add a polling loop or generic DOM observer.
- [x] 2.5 Subscribe per App instance to the existing coarse-pointer media query change event, feed it only into density, and clean it up. On the first canvas pen/touch pointer, latch `isTouchScreen` and refresh the profile immediately while preserving separate pen-mode state, pointer actions, and collaboration timing.
- [x] 2.6 Add App lifecycle tests for initial/no-size/first-valid measurement, positive resize without window resize, zero/detach/reattach, fixed-size language direction, coarse-query changes, touch/pen latch, visual-viewport/safe-area refresh, equal-signature object reuse, unmount cleanup, and two simultaneous editors with different bounds/policies/input/safe areas.

## 3. Migrate adapters and semantic consumers without changing ownership

- [x] 3.1 Route `LayerUI`'s existing desktop/phone branch through `profile.adapter`, retaining tablet on desktop and removing stale adapter DOM on a real swap. Preserve all current render gating, tunnels, toolbar/property/sidebar actions and open state, host preferences, collaboration state/timing, and container-local bounds.
- [x] 3.2 Replace ad hoc common-caller form-factor/direction/density/presentation combinations with `useResponsiveEditorShell()` only where the profile owns the fact. In particular, make Modal and other memoized large/floating policy consumers refresh on fixed-size direction/input/safe-area changes; do not move focus, modal claims, scrolling, or domain lifecycle into the profile.
- [x] 3.3 Feed AppContent and the delivered toolbar, property, CanvasUiLayout, sidebar, floating, and large-surface adapters only the semantic profile facts they need. Keep AppContent deep/internal and absent from the public package entry; leave inventories, actions, selection/open state, content, placement lifecycle, and overflow ownership in their existing modules.
- [x] 3.4 Add integration coverage for desktop↔tablet (same adapter) and desktop/tablet↔phone swaps with representative selected tool, property state, sidebar/domain surface, host content, and collaboration state. Assert current adapter DOM only, preserved action/state semantics, and no duplicate tunnel, surface, or viewport marker.
- [x] 3.5 Add a `LayerUI` custom-memo regression that changes direction, density, safe area, tier, adapter, presentation, orientation, and short-block state with otherwise referentially equal compared props; assert context consumers refresh without a new prop or shallow identity contract.

## 4. Canonicalize safe areas and named reservation freshness

- [x] 4.1 Make root `--sat`, `--sar`, `--sab`, and `--sal` the only environment-backed physical safe-area inputs. Convert floating- and large-surface variable names to compatibility aliases of the base values and replace AppContent's direct `env(safe-area-inset-*)` reads; introduce no new design token or `!important`.
- [x] 4.2 Route JavaScript floating/large collision policy and responsive profile reads through the same canonical physical values, using logical projections only for logical start/end routing. Preserve editor-container bounds and verify direction never flips the physical inset object.
- [x] 4.3 Generalize `AppViewport.uiLastMeasured` to normalized named entries containing physical dock, offset, adapter, direction, presentation, and complete profile signature. Use one entry creator/validator for every `ViewportUIName`; delete invalid entries before choosing deterministic fallback.
- [x] 4.4 Migrate both styles-panel and sidebar hidden reservation paths to the generalized validation, retaining rendered leaves as authority, retaining phone's no-hidden-desktop-side-reservation behavior, and keeping CanvasUiLayout root/rows/zones pointer- and marker-neutral.
- [x] 4.5 Extend CSS/policy tests with asymmetric LTR/RTL values and one base-variable update shared by layout, floating, large, AppContent, toast/status, and JavaScript consumers. Assert no independent environment authority remains and constrained embeds stay editor-bounded.
- [x] 4.6 Extend viewport tests for finite/nonnegative offset normalization; rendered measurement writes; valid reuse; physical dock, direction, adapter, presentation, and unrelated signature-field invalidation; sidebar/styles-panel parity; phone fallback; leaf-only markers; and isolation between two AppViewport instances.

## 5. Lock accessibility, overflow, pointer, and recovery behavior

- [x] 5.1 Verify every supported profile delegates narrow fit to the owning toolbar/surface/sidebar adapter's existing overflow, wrap, internal scroll, overlay, sheet, or fullscreen behavior. Add regression assertions that editor `scrollWidth` does not exceed `clientWidth`, no established touch/keyboard target shrinks, and no valid action is clipped without a reachable route.
- [x] 5.2 Add accessibility integration coverage across compact/touch density, LTR/RTL, open surface, and adapter swap: accessible names/roles, focus visibility, keyboard activation/order, shortcuts, selected/expanded state, focus return, and no hidden or unrelated synthesized focus when focused adapter DOM is removed.
- [x] 5.3 Re-run/extend canvas-layout integration coverage so empty root/row/zone areas still deliver a complete drawing pointer sequence after profile changes, real controls still honor `--ui-pointerEvents`, and only actual occluding leaves retain `data-viewport-ui`/`data-viewport-ui-name`.
- [x] 5.4 Fix only the narrow `TopErrorBoundary` recovery overflow: retain the existing 762px card width when space permits, bound the frame at narrow sizes, and make every recovery detail/action reachable without whole-editor horizontal scrolling. Add focused desktop-width and narrow-overflow assertions.
- [x] 5.5 Review adapter transition tests for ownership explicitly: the resolver must not own toolbar inventory/overflow, property actions/open state, sidebar or large/floating lifecycle/scroll, AppContent data/actions, host preference, collaboration timing, or persisted state.

## 6. Implementation-owner real-Chrome acceptance only

- [x] 6.1 Start from a clean app reload in a disposable CDP target and capture implementer evidence for 1440×900 light/LTR and dark/RTL with profile/adapter hooks, selected tool/property controls, an open floating/large surface, focus/ARIA state, bounds, leaf reservations, an empty-canvas hit point, and horizontal-scroll metrics.
- [x] 6.2 Capture 1024×768 dark/RTL with Library/sidebar on the expected physical side, 768×1024 light/LTR with compact property rail, and 1180×700 light/LTR embed with AI/large sheet content. Record physical/logical safe areas, surface/container bounds, profile signature, and no overlap, clipping, stale cache, or horizontal editor scroll.
- [x] 6.3 Capture 812×375 dark/RTL and 375×812 light/LTR phone states, including open Library, centered bottom stack, a real synthetic touch/pen path or documented test-harness latch, target geometry, keyboard/focus/ARIA state, canvas input beside chrome, and absence of stale desktop controls.
- [x] 6.4 Capture a 640×480 dark/RTL embed with deliberately asymmetric nonzero base safe areas and open Help/large content. Prove all surface families and JavaScript policy reflect one physical authority, logical edges mirror correctly, content remains editor-bounded/reachable, and direction does not flip physical values.
- [x] 6.5 Hard-reload the final implementer state and record zero checker messages, visible error overlays, and error-level console entries. Save screenshots plus DOM/profile/geometry/scroll/console notes under `rasen/changes/responsive-editor-shell/evidence/implementation-chrome/`, close the disposable target, confirm it is absent, and do not stop the shared proxy.

## 7. Automated verification range only

- [x] 7.1 Run the focused resolver, App/context lifecycle, LayerUI, CanvasUiLayout, viewport reservation, floating/large/AppContent policy, TopErrorBoundary, accessibility, pointer, and multi-editor test files introduced or changed above. Update only intentional snapshots and inspect every delta for lost controls, labels, actions, tunnels, or markers.
- [x] 7.2 Run focused lint and formatting checks over changed TypeScript/TSX/SCSS/tests/artifacts plus strict UTF-8/no-BOM/U+FFFD/mojibake checks. Fix root causes; do not perform unrelated whole-file rewrites.
- [x] 7.3 Run `yarn build:excalidraw` first, then `yarn test:typecheck`, because generated package output is a root typecheck input. Record exact commands and outcomes in implementation evidence and do not substitute an unrelated flaky full suite for the focused acceptance gate.
- [x] 7.4 Run `rasen validate responsive-editor-shell --strict --json`, checkbox/duplicate-task-ID checks, and `git diff --check`; record clean machine-readable outcomes before requesting independent review.

## 8. Independent reviewer range only

- [x] 8.1 A reviewer distinct from the implementer reads proposal/design/spec/tasks and reviews the complete branch diff with codebase context. Verify the exact resolver/profile interface, deletion test, single observer/context architecture, ownership table, migration cut lines, normalization, listener cleanup, memo freshness, and absence of public/API/dependency expansion; do not edit code in this range.
- [x] 8.2 Independently inspect automated evidence and run risk-directed focused tests for threshold neighbors, host overrides, detach/zero/reattach, fixed-size direction/input/safe-area, context identity/freshness, marker/pointer neutrality, reservation invalidation, adapter state preservation, cleanup, and multi-editor isolation. Report severity-tagged findings with file/line and reproducible evidence; do not self-fix.
- [x] 8.3 In fresh disposable Chrome targets, independently repeat the full 1440×900, 1024×768, 768×1024, 1180×700, 812×375, 375×812, and asymmetric-safe-area 640×480 matrix from section 6 in both required themes/directions/states. Exercise controls and canvas directly rather than approving implementer screenshots.
- [x] 8.4 Save reviewer screenshots plus DOM/profile/ARIA/geometry/scroll/reservation/console evidence under `rasen/changes/responsive-editor-shell/evidence/review-chrome/`; hard-reload and record zero checker messages, visible error overlays, and error-level console entries; close and confirm all reviewer targets absent without stopping the proxy.
- [x] 8.5 Issue an explicit PASS or a finite finding list. Confirm there is no whole-editor horizontal-scroll fallback, target shrinkage, stale physical/logical safe area, stale sidebar/property reservation, adapter-owned state loss, second observer/provider/store, public export, generic capability-rule compiler, or final screenshot-regression infrastructure.

## 9. Fixer and delta re-review range only

- [x] 9.1 If independent review reports findings, a fixer distinct from that reviewer changes only confirmed in-scope root causes and adds the smallest regression coverage. Preserve the locked resolver interface, ownership boundaries, migration cut lines, user/unrelated worktree changes, and previously passing behavior; record each finding-to-fix mapping.
- [x] 9.2 Re-run only checks and Chrome states affected by the fixer delta first, then the section 7 gates if the delta can affect them. Store new evidence separately; do not overwrite the implementer or original reviewer record.
- [x] 9.3 The same independent reviewer re-reviews only the fixer delta and targeted evidence, then repeats the affected browser interactions. Return PASS or a smaller finite finding list; the fixer must not approve its own work.
- [x] 9.4 Repeat 9.1–9.3 only until PASS or the review-cycle cap requires explicit human escalation. Do not dilute, reclassify, or silently defer unresolved correctness, accessibility, ownership, or responsive-boundary findings.

## 10. Final audits and local delivery only

- [x] 10.1 After reviewer PASS, run one final `rasen validate responsive-editor-shell --strict --json`, checkbox count/duplicate-ID audit, strict UTF-8/no-BOM/U+FFFD/mojibake validation, JSON/TOML/YAML syntax checks for any changed structured files, and `git diff --check`.
- [x] 10.2 Audit the final diff and untracked task files for zero prohibited additions: external/copy reference terms or assessment filenames, copied source/CSS/selectors/icons/fonts/assets, dependencies, public exports, new design tokens, new `!important`, generic capability compiler/store/provider/observer, whole-editor horizontal scroll, target shrinkage, final screenshot-regression infrastructure, or unrelated edits.
- [x] 10.3 Confirm the full product/test diff matches the reviewed implementation exactly, all disposable Chrome targets are absent, the shared proxy remains running, and evidence includes implementer and independent reviewer matrices plus automated command outcomes.
- [x] 10.4 Prepare local delivery only through the repository's ship workflow after all gates pass: include the proposal-derived summary and evidence paths, do not archive or sync specs in this child, and do not push or open a PR unless the parent workflow explicitly selects that delivery mode.
